import 'package:flutter/material.dart';

class SchedulesScreens extends StatelessWidget {
  const SchedulesScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
